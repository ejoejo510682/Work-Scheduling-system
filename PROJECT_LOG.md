# 開發進度紀錄

## 2026/08/07

- 完成規劃書（v0.8），確認所有核心功能設計：崗位與能力（三級制）、5 個時段結構、崗位×時段對照、崗位標準需求人力、每日可上班範圍（含半天班）、週排班＋臨時異動邏輯、本週追加任務、班表匯出 PDF、新增崗位/PT 流程
- Phase 0 環境建置開始：
  - 用 `create-next-app` 建立 Next.js 專案（TypeScript + Tailwind + App Router + src 目錄），資料夾位置 `C:\Users\Cynthia\Desktop\claude project\排班系統`
  - 建立 CLAUDE.md、SKILL.md、PROJECT_LOG.md
  - 待辦：git 初始化、GitHub repo、Supabase 專案（使用者自行建立後提供金鑰）
- git repo 初始化並完成第一個 commit（身份沿用訂餐系統：ejoejo510682）
- GitHub remote 設定完成：https://github.com/ejoejo510682/Work-Scheduling-system（尚未 push，待使用者確認）
- 寫好第一份資料庫 migration `supabase/migrations/0001_init.sql`：12 張表 + RLS 政策（主管全權限；排班人員可寫入可上班範圍／班表／追加任務指派；兩者皆可讀取）。同一 date+slot+pt_id 用 unique constraint 防止重複指派。待使用者提供 Supabase 專案金鑰後即可執行
- Supabase 專案建立完成（沿用 lunch-order-system 這個 organization），`.env.local` 已設定，migration 已執行並驗證 RLS 正常運作
- 建立第一個主管帳號：ejoejo510682@gmail.com（Cynthia，role=主管），已寫入 admin_users 表
- 完成登入頁面（/login）與後台外殼（/admin，requireRole 檢查登入與角色，顯示姓名/角色/登出）；根目錄 / 依登入狀態自動導向
- 密碼錯誤的錯誤提示已用瀏覽器測試確認正常；登入成功流程待使用者自行用密碼測試
- Phase 1 完成：
  - 直接用已確認的規劃資料，透過 Supabase API 建好 7 個崗位、對應訓練項目、標準需求人力、崗位×時段對照，以及 7 位 PT 名單（姜文雯、王瀞玉、張智為、楊鎔慈、李睿懷、熊庭漢、黃品甄）
  - 建立 /admin/positions（崗位管理，含新增）、/admin/pt（PT 名單管理，含新增）、/admin/abilities（能力等級 7×7 表格，直接調整一/二/三級）三個主管專用頁面
  - 後台導覽列依角色顯示不同連結
  - 使用者已用真實帳密測試登入成功
- 下一步：Phase 2 能力總表查詢頁（給主管與排班人員都能看的唯讀版本）
- 已部署到 Vercel：https://work-scheduling-system.vercel.app （使用者自行完成 GitHub/Vercel 連動與環境變數設定；已用瀏覽器確認登入頁正常運作、無錯誤）
- Phase 2 完成：/admin/ability-summary 能力總表查詢頁（主管與排班人員皆可看，唯讀彩色標籤），導覽列同步更新
- 下一步：Phase 3 訓練紀錄與升等核准
- Phase 3 完成：/admin/training 訓練紀錄頁（主管專用）——登記受訓自動把能力調成二級，核准完訓自動調成三級並記錄核准人/時間
- 下一步：Phase 4 可上班範圍登記（含半天班）
- Phase 4 完成：/admin/availability 週檢視表格（PT × 7 天），可設全天/只上午/只下午/全天休假，上一週/下一週切換，主管與排班人員皆可編輯
- 下一步：Phase 5 週排班表＋建議名單＋臨時異動替補（最核心的部分）
- Phase 5 完成：
  - /admin/schedule 週總覽（7 天卡片）+ /admin/schedule/[date] 單日排班頁
  - 依 5 個時段列出當時開工的崗位，「處理團購訂單」這種沒綁固定時段的崗位獨立顯示為「上午/下午整個半天」彈性區塊
  - 建議名單：篩選能力等級（二/三級）＋ 當時段可上班 ＋ 未被排進同時段其他崗位，三級優先排序；掛零時顯示紅色「人力缺口」警示
  - 防重複指派：靠 daily_schedule 的 unique(date, slot, pt_id) + 前端篩選雙重把關，衝突時給友善錯誤訊息
  - 臨時異動：已排的人若當天可上班範圍不再涵蓋這個時段（例如臨時請假/半天），該人的名牌會變紅並標示「今天請假/半天」，排班人員可直接移除並從建議名單重新挑人——跟例行排班共用同一套邏輯，不用另外學一套操作
- 下一步：Phase 6 本週追加任務
- 修正 bug：可上班範圍、能力等級頁面存檔沒有檢查錯誤也沒有 router.refresh()，導致切頁後看起來像是「設定消失」；已補上錯誤提示與 refresh
- 新增功能：第一/第二任務（備援）——daily_schedule 新增 priority 欄位（migration 0002，已執行），同一時段內一個人可以有第一任務+一個備援任務；備援候選只從「這個時段已有第一任務、還沒有備援」的人裡面選，且一樣套用能力等級篩選；備援算進該崗位的已排人數，用藍色標籤與「（備援）」文字跟正式任務區分
- 修正真正的 root cause：可上班範圍頁用 `?week=` 切換上一週/下一週時，React 沒有真的重新掛載 AvailabilityGrid 元件（同一個元件實例、只是換了 props），元件內部用 useState 存的畫面資料不會自動跟著新的一週重新初始化，導致切週之後畫面顯示舊資料/預設值，但資料庫其實是對的。修法：在 `<AvailabilityGrid key={monday} .../>` 加上 key，讓 React 換週時強制整個重新掛載。查資料庫確認過使用者回報「設定不見」那次，資料其實都有正確存進去，純粹是畫面顯示的問題
- Phase 6 完成：/admin/weekly-tasks 本週追加任務頁——主管建立任務（可選填需要哪個崗位、至少幾級），主管與排班人員都能指派人員、更新待處理/進行中/已完成狀態；指派候選會依任務設定的能力門檻篩選
- 新增體驗優化：平日預設顯示「這一週」，週六日預設自動跳到「下一週」（可上班範圍、週排班表、本週追加任務三個頁面都套用），避免假日規劃下週班表時還要手動點下一週
- 下一步：Phase 7 班表檢視／列印／匯出 PDF（最後一個 Phase）
- Phase 7 完成（規劃書 7 個 Phase 全部做完）：
  - /admin/schedule/print：崗位 × 7 天的緊湊表格，每格列出當天各時段的指派名單（備援標註「(備援)」），彈性崗位（團購）顯示上午/下午
  - 用瀏覽器內建的列印功能做 PDF 匯出（按「🖨 列印／匯出 PDF」開瀏覽器列印對話框，選「另存為 PDF」），不用額外套件，維持輕量
  - 列印時用 CSS 自動隱藏後台導覽列與操作按鈕，只留班表本身
- 8 個 Phase（0～7）全部完成，規劃書列出的核心功能都已上線
- 修正嚴重 bug：多個後台頁面（含本週追加任務、週排班表）會隨機出現 500「This page couldn't load」。用 Vercel Logs 查到真正原因：Supabase 登入 session 快過期時會在頁面渲染途中嘗試更新 cookie，但 Next.js 規定「一般頁面渲染中不能改 cookie」，就直接把整頁請求爆掉，不是特定頁面的問題，是所有頁面共用的登入機制有洞。修法兩步：
  1. `lib/supabase/server.ts` 的 `setAll` 包 try/catch，被擋下時不要整頁當機
  2. 新增 `src/proxy.ts`（這個 Next.js 版本把 `middleware.ts` 改名成 `proxy.ts`），在進 /admin 每個頁面前先幫 session 續期，從源頭大幅降低觸發機會
  這是這台 Next.js 版本第一次真的踩到 AGENTS.md 警告的「跟你認知的 Next.js 不一樣」的地雷（middleware → proxy 改名）
- 班表列印/匯出 PDF 排版改版：使用者提供原本手動維護的 Excel 週排程表當參考（人員分組、依時段上色），把 /admin/schedule/print 從「崗位 x 日期」的表格改成「人員 x 日期」，每位 PT 一個區塊、5 個時段各一列，時段用顏色區分（上午出訂單前=紅、上午出訂單後=橘、下午出訂單前=藍、下午出訂單後=綠、出貨完成後=灰），跟原本熟悉的配色風格對齊
- 依使用者回饋再調整列印配色：(1) 每個儲存格文字顏色改成明確指定，不再依賴繼承，避免淺底配淺字看不清楚 (2) 時間節點欄位不再重複標示上午/下午（時段欄已經有了），只寫「出訂單前/出訂單後/出貨完成後」 (3) 顏色從 4 種色系縮減成單一藍色色系的深淺兩階（出訂單前/出訂單後、上午/下午都共用），只有「出貨完成後」用灰色跳出來 (4) 加上 print-color-adjust: exact，避免瀏覽器列印預設不印底色，導致顏色在真正匯出的 PDF 裡消失
- 崗位管理加上編輯功能：崗位名稱、標準需求人力都可以直接改（改名時同步更新自動建立的訓練項目名稱，避免兩邊對不上）
- 首頁「今日總覽」從 Phase 1 留下的「建置中」佔位文字，改成真正的儀表板：本週追加任務清單與進度、「更新可上班範圍」「本週排班」兩個快速按鈕
- 新增「正職協助出貨」支援：因為排班系統原本只給 PT 用，但正職有時也會支援出貨工作。做法是在 `pt_staff` 加一個 `employment_type`（PT／正職）欄位，不另外開一張表——能力等級、可上班範圍、排班指派全部沿用同一套邏輯，正職只是人員清單裡的另一種身份。所有列出人員的畫面（人員名單、能力總表、能力等級設定、可上班範圍、排班、追加任務、訓練紀錄、列印PDF）名字旁邊都加上「PT」/「正職」小標籤方便辨識；「PT 名單」也順便改名成「人員名單」，migration 檔是 `supabase/migrations/0003_employment_type.sql`
- 新增「PT／正職自助填寫下個月可上班日期」：因為工作天數要擴大到一週6~7天、人數也變多，靠後台人工一個一個登記成本太高。做法是新增第一個對外公開頁面 `/my-availability`（不用登入，共用連結進去選自己的名字，瀏覽器 localStorage 記住上次選誰），開放填寫「下個月」整月的可上班範圍，資料寫進跟後台「可上班範圍」共用的同一張 `pt_daily_availability` 表，不用審核直接生效，後台隨時還是可以手動覆蓋。安全上沒有開放 anon key 直接讀寫資料庫、也沒改 RLS——名單讀取跟寫入都是伺服器端用 `createServiceRoleClient()` 代為處理（新增了第一個 API Route `/api/my-availability`），前端只是體驗，時間窗真正的把關在 API Route 裡重新驗證一次。這頁刻意加了 `export const dynamic = "force-dynamic"`，因為完全沒用到 cookies()/searchParams，不強制的話 Next.js 可能會把「今天開不開放」這個判斷凍結在部署當下，之後永遠不會更新
- 調整開放時間規則：原本設計「每個月最後一個週末」開放，使用者回饋太晚、排班人員沒時間準備，改成**每個月 1～20 號**都能填寫/修改下個月的班表，超過 20 號就鎖住，留 21 號到月底這段時間給排班人員拿確定的資料排下個月的班。判斷邏輯簡化成單純比較「今天是幾號」，不用再算週六日
- 上線這天（2026/8/27）已經過了 8 月的 20 號，為了讓大家能先把 9 月的班表補填完，在 `getSubmissionWindow` 加了一段**一次性例外**：8/21～8/29 照樣開放（填 9 月），過了 8/29 就照正常規則鎖到 9/1。這段例外之後不會再符合條件，之後可以直接刪掉
- 順便修正時區風險：Vercel 伺服器不一定跑在台灣時區，直接用 `new Date()` 判斷「今天幾號」可能跟台灣時間差到 8 小時，導致鎖住的時間點跟預期的午夜對不上。新增 `todayInTaipei()`（用 `Intl.DateTimeFormat` 明確指定 Asia/Taipei）取代這個頁面原本用的 `toDateStr(new Date())`
- 單日排班頁加「← 前一天／後一天 →」箭頭，可以直接跳日期排班不用先跳回週總覽；跨週也照樣移動（已經在排假日班，不會跳過六日）
- 排班時加上「一週已排幾天」警示：候選名單、已排名牌會顯示這個人這週已經排了幾天班，達到或超過 4 天（`MAX_DAYS_PER_WEEK`）變紅字警示，但不擋下指派，只提醒；**只對 PT 顯示**，正職沒有這個限制
- 新增「假日與檔期」提醒：`/admin/special-dates` 後台頁面管理國定假日、以及規律以外的自訂電商檔期（例如黑色星期五），新表 `special_dates`（migration `0004_special_dates.sql`）。「每月雙字日／18號／25號」這種每月固定重複的電商檔期規律不用存資料庫，寫成 `src/lib/specialDates.ts` 裡的一個純函式直接算，不用每年維護也不會過期。週排班總覽的日期卡片、單日排班頁最上面都會顯示提醒。2026年剩下的台灣國定假日（含補假）我先幫忙查好官方行事曆建進資料庫
- 人員名單加上改名功能：姓名旁邊加「編輯」，可以直接改名字，跟之前崗位管理改名字的做法一樣。人員的姓名沒有像崗位那樣有另一張表存副本，所以單純改一個欄位，不用同步別的地方
- 新增「帳號管理」頁面（`/admin/accounts`，主管專用）：規劃書一開始就列在頁面清單裡但一直沒做，之前新增帳號都要我手動到 Supabase 後台操作兩個地方（Authentication 開帳密 + admin_users 表填角色）。現在主管可以直接在系統裡填姓名/email/密碼/角色，按新增就好。這是第一個需要用到 Supabase 帳號管理 API（`auth.admin.createUser`）的功能，跟一般資料表的新增不一樣，所以另外開了 `/api/admin-accounts` 這個 API Route，用 service role 在後端建立登入帳號，並在 API 裡重新驗證呼叫者真的是主管（不能只靠前端頁面擋）；如果建立帳密成功但寫入 admin_users 失敗，會自動把剛剛建立的登入帳密刪掉，不會留下無法登入對應資料的孤兒帳號。用一個真實的測試帳號跑過一次完整流程（建立→寫入→確認讀得到→刪除→確認真的清乾淨），確認沒問題後把測試帳號完全刪除

@AGENTS.md
@SKILL.md

# 排班系統（PT 能力排班系統）

> 本檔提供給 Claude Code 在每次新對話開始時閱讀，建立對專案的整體理解。
> 本專案與同一台電腦上的「訂餐系統」完全獨立，兩者無任何共用資料或程式碼。

## 一、專案目標

讓排班人員快速掌握公司所有 PT（工讀生）具備的能力與熟練程度，安排每週班表時能自動列出符合資格的候補人選；同時支援臨時請假的快速替補，最終目標是完全取代目前用 Excel 排班的方式。

## 二、角色

| 角色 | 人數 | 職責 |
|------|:---:|------|
| 主管 | 1 位 | 全權限：PT／能力／訓練紀錄管理、崗位與人力需求設定、追加任務建立、帳號管理，也能排班 |
| 排班人員 | 1 位 | 週排班、臨時異動替補、可上班範圍登記、追加任務指派與進度更新、查詢能力總表 |

兩人各自登入、分權限。詳細權限矩陣見下方。

## 三、技術棧

| 層級 | 技術 |
|------|------|
| 前端 + 後台 | Next.js（App Router）+ Tailwind CSS，部署 Vercel |
| 後端 API | Next.js API Routes |
| 資料庫 | Supabase (PostgreSQL) |
| 認證 | Supabase Auth（Email + 密碼） |

預估月成本：NT$0。

## 四、核心概念

### 4.1 崗位與能力（7 項，一對一）

包裝、撿貨、印訂單、進貨、無包裝出貨、處理團購訂單、出箱貨。

崗位清單不是寫死的，主管可以在「基礎資料設定」新增崗位；新增崗位時系統要一併建立同名能力／訓練項目，並要求主管補上標準需求人力與適用時段。

### 4.2 能力等級（三級制）

- **一級**：完全不會
- **二級**：訓練中
- **三級**：可獨立執行

與訓練紀錄連動：開始受訓 → 自動變二級；主管核准完訓 → 調整為三級。主管也可以不透過訓練紀錄，直接手動設定/調整任何人的等級——用於系統初次上線時的資料建置，以及日後偶爾需要的微調（不需要額外的「降級」流程，共用同一個入口）。

### 4.3 一天 5 個時段

不是單純「上午/下午」，而是照工作流程細分：

```
上午：出訂單前 → 出訂單後
下午：出訂單前 → 出訂單後
全天收尾：出貨完成後
```

**崗位 × 時段對照**（哪個崗位在哪個時段開工）：

| 崗位 | 上午出訂單前 | 上午出訂單後 | 下午出訂單前 | 下午出訂單後 | 出貨完成後 |
|---|:---:|:---:|:---:|:---:|:---:|
| 印訂單 | ✓ | | ✓ | | |
| 撿貨 | | ✓ | | ✓ | |
| 包裝 | | ✓ | | ✓ | |
| 無包裝出貨 | | | | ✓ | |
| 進貨 | ✓ | | ✓ | | ✓ |
| 出箱貨 | | ✓ | | ✓ | |

處理團購訂單不綁固定時段：彈性佔用上午或下午「整個半天」，由排班人員當天決定。

**同一時段（5 個時段裡的同一格）不能把同一人排進兩個不同崗位**——系統要擋下這種重複指派。但同一個上午/下午內，只要不是同時做兩件事，允許換崗位（例如上午先印訂單，訂單出來後接著去撿貨）。

### 4.4 崗位標準需求人力

包裝 2 人・撿貨 3 人・印訂單 1 人・進貨 2 人・無包裝出貨 2 人・處理團購訂單 1 人・出箱貨 2 人。這是「開工時」的標準人力，不分時段。

### 4.5 每日可上班範圍（取代單純的「休假」）

每人每天設定一次，統一處理半天班／臨時半天請假／整天請假三種情況，不分開設計：

- **全天**（預設值）
- **只上午** — 下午時段自動排除
- **只下午** — 上午時段自動排除
- **全天休假** — 整天的時段都排除

半天班的人每天可能不一樣（例如週一只上午、週三只下午），排班人員每週依當週狀況個別調整。

### 4.6 建議名單邏輯

選時段、崗位 → 系統篩選「該崗位等級為三級或二級」＋「這個時段對應的半天有上班」→ 三級優先列出，二級（訓練中）也顯示並標示需搭配資深人員 → 排班人員勾選確認，顯示「已排／需求」人數。

**若篩選結果掛零**（沒有任何符合資格的人），畫面要用明顯的「人力缺口」警示標示，不能安靜顯示空名單。

### 4.7 排班節奏：週排班 + 臨時異動

- **週排班（例行）**：每週開始前，一次排完下週 7 天、每天 5 個時段、7 個崗位的人力。
- **臨時異動（緊急替補）**：把某人當天的可上班範圍改成休假/半天 → 系統自動列出他當天受影響的時段與崗位 → 針對每個受影響時段重新叫出建議名單 → 排班人員勾選替補。兩種情境共用同一套候補邏輯。

### 4.8 本週追加任務

除了 7 個日常崗位，每週可能有臨時的額外任務（例如整理倉庫、盤點）。

- 任務項目由**主管**建立，可選擇性設定「需要哪個崗位能力、至少幾級」作為門檻
- **主管或排班人員**指派給符合資格的 PT，一個任務可以指派給多人
- 進度狀態：待處理／進行中／已完成
- 任務只綁定「本週」，不指定週內哪一天完成

### 4.9 訓練紀錄

訓練項目跟 7 個崗位一一對應：包裝訓練、撿貨訓練、印單訓練、進貨訓練、無包裝訓練、團購訂單出貨訓練、箱出貨訓練。記錄每位 PT 上過哪個項目、進度如何。

### 4.10 班表匯出

週班表要能匯出成 PDF，方便列印張貼或傳給別人看。

### 4.11 新增/停用 PT

新增 PT：所有崗位（含未來新增的）預設「一級」，每日可上班範圍預設「全天」。
PT 離職：用「停用」而非直接刪除，保留過去班表紀錄；停用後不會出現在建議名單裡。

## 五、資料庫 Schema 摘要

> 完整欄位以 Supabase migration 檔為準，此處列出設計重點。

1. **positions**（崗位）— id / name / is_active / sort_order
2. **pt_staff**（PT 人員）— id / name / is_active
3. **pt_abilities**（PT 能力對照）— id / pt_id (FK) / position_id (FK) / level (1/2/3)
4. **training_items**（訓練項目清單）— id / position_id (FK) / name
5. **training_records**（訓練紀錄）— id / pt_id (FK) / training_item_id (FK) / status（進行中/已完成）/ approved_by / approved_at / note
6. **pt_daily_availability**（每日可上班範圍）— id / pt_id (FK) / date / range（全天/只上午/只下午/全天休假）
7. **position_headcount**（崗位標準需求人力）— id / position_id (FK) / count
8. **position_slot_map**（崗位 × 時段對照）— id / position_id (FK) / slot（5 選項之一）
9. **daily_schedule**（每日班表）— id / date / slot（5 選項之一）/ position_id (FK) / pt_id (FK)
10. **weekly_tasks**（本週追加任務）— id / week_start / name / note / required_position_id (nullable FK) / required_level (nullable) / status / created_by
11. **weekly_task_assignments**（追加任務指派）— id / task_id (FK) / pt_id (FK) / assigned_by
12. **admin_users**（後台帳號）— id (對應 Supabase Auth) / email / name / role（主管/排班人員）

### 關鍵設計決策

- **能力等級**：優先由訓練紀錄核准驅動（二級 → 三級），但主管保留直接手動調整的入口，涵蓋初始資料建置與例外微調。
- **每日可上班範圍**：單一欄位涵蓋半天班／臨時半天請假／整天請假，不做成三張分開的表。
- **同時段防重複指派**：由後端 API 在寫入 daily_schedule 時擋下（同一 date + slot + pt_id 只能對應一個 position_id）。
- **PT 停用不刪除**：`pt_staff.is_active`，離職 PT 保留歷史班表紀錄。

## 六、頁面規劃

**共用**：登入頁・首頁今日總覽・能力總表・班表查詢／列印／匯出 PDF
**排班操作**：週排班總覽頁（含建議名單、臨時異動快速替補）・可上班範圍登記頁・追加任務指派頁
**主管專用**：PT 與能力管理・訓練紀錄管理・崗位與人力需求設定・本週追加任務建立・帳號管理

## 七、開發階段

| Phase | 內容 |
|-------|------|
| 0 | 環境建置（Next.js + Supabase + Vercel） |
| 1 | 基礎資料（崗位、能力、PT 名單、帳號登入）+ 能力等級初始資料建置 |
| 2 | 能力總表查詢 |
| 3 | 訓練紀錄與升等核准 |
| 4 | 可上班範圍登記（含半天班） |
| 5 | 週排班表＋建議名單＋臨時異動替補 |
| 6 | 本週追加任務 |
| 7 | 班表檢視／列印／匯出 PDF |

## 八、GitHub Repo

https://github.com/ejoejo510682/Work-Scheduling-system

## 九、配套文件

- **PROJECT_LOG.md**：開發進度與決策記錄（每完成一個小步驟就更新）
- **SKILL.md**：本專案專屬的開發守則
